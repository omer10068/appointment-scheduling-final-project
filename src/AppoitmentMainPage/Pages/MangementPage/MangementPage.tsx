import { Box } from "@mui/material";
import React from "react";

export default function MangementPage() {
  return <Box sx={{width: '100%', height: '100vh', display: 'flex', flexDirection: 'row', justifyContent: 'center'}}>
    MangementPage - Yosef
  </Box>;
}
